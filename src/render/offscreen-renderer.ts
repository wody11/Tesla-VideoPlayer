/*
 * OffscreenCanvas rendering placeholder. It is disabled on the main no-video
 * path until the worker rendering path is connected.
 */

export class OffscreenRenderer {
  readonly disabledReason = 'OffscreenCanvas renderer is TODO; main-thread Canvas/WebGL renderers are active.';
}

