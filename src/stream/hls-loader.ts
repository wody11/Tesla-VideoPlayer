/*
 * HLS loading is handled in worker-entry for no-video playback.
 * This facade exists so the public architecture matches the Jessibuca style.
 */

export class HlsLoader {
  readonly disabledReason = '';
}

